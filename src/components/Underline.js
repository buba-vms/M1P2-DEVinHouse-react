import React from 'react';

const Underline = () => {
  return (
    <div
      className="bg-secondary"
      style={{
        height: '4px',
        width: '100%',
        margin: '15px auto',
        borderRadius: '18px',
      }}
    ></div>
  );
};

export default Underline;
